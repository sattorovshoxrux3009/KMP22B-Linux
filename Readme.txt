sudo apt remove brltty  // Ushbu dasturni o'chirish kerak. Arduino har uzilib ulanganida portni ochishga qarshilik qiladi.
sudo apt update
sudo apt install libcurl4-openssl-dev      // SSL ni o'rnatish shart!!!   curl orqali GET va POST so'rovi yuborish uchun
